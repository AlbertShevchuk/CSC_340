#include <iostream>
#include <string.h>

using namespace std;

void getLetterFreq( char token)
{
	cout << "hello dude" << endl << ;
}

int main()
{
  char token[100];
  
  void getLetterFreq(ptr);

  //fgets(src, sizeof(src), stdin);
  //printf("%s\n",strcpy(dest, src, allow));
  return 0;
}
